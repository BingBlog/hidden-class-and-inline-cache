function add(x, y) {
    return x + y;
}

add(1, 2);

// Warm up with SignedSmall feedback.
// % DebugPrint(add);
// % OptimizeFunctionOnNextCall(add);
// add(1, 2); // Optimize and run generated code.
// % DebugPrint(add);
